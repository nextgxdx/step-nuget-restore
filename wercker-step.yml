name: nuget-restore
version: 0.0.1
description: Executes the `nuget restore` command leveraging the wercker cache.
keywords:
  - c#
  - .net
  - mono
  - nuget
  - restore
properties:
  options:
    type: string
    required: false
